import { Component } from 'react';
import { ListItems } from '../../components';

export class Main extends Component {
  render(): JSX.Element {
    return (
      <main>
        <ListItems />
      </main>
    );
  }
}
